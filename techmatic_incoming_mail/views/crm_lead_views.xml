<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Smart button: jump from the lead to its incoming customer emails. -->
    <record id="crm_lead_view_form_incoming_mail" model="ir.ui.view">
        <field name="name">crm.lead.form.incoming.mail</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_incoming_mails" type="object"
                        class="oe_stat_button" icon="fa-envelope-o"
                        invisible="incoming_mail_count == 0">
                    <field name="incoming_mail_count" widget="statinfo"
                           string="Incoming Mails"/>
                </button>
            </xpath>
        </field>
    </record>

</odoo>
